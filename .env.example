# =============================================================================
# Variáveis de Ambiente - Messaging Standalone SIFEN
# =============================================================================
# Copie este arquivo para .env e preencha com seus valores:
#   cp .env.example .env
# =============================================================================

# =============================================================================
# BANCO DE DADOS ORACLE
# =============================================================================
# Opção 1: String de conexão completa (formato: user/password@host:port/service_name)
ORACLE_CONNECTION_STRING=user/password@host:1521/service_name

# Opção 2: Componentes separados (usado se ORACLE_CONNECTION_STRING não estiver definido)
# ORACLE_USER=usuario
# ORACLE_PASSWORD=senha
# ORACLE_DSN=host:1521/service_name

# NOTA DOCKER: Se o Oracle está rodando na máquina host, use:
#   - Linux: o IP da interface docker0 (ex: 172.17.0.1)
#   - Mac/Windows: host.docker.internal
# Exemplo: ORACLE_CONNECTION_STRING=user/pass@host.docker.internal:1521/service

# =============================================================================
# RABBITMQ
# =============================================================================
# Estas credenciais são usadas tanto para criar o usuário no RabbitMQ
# quanto para o worker se conectar
RABBITMQ_HOST=rabbitmq
RABBITMQ_PORT=5672
RABBITMQ_USER=sifen_user
RABBITMQ_PASS=sifen_pass_segura

# Portas expostas (apenas para docker-compose)
RABBITMQ_EXTERNAL_PORT=5672
RABBITMQ_MGMT_PORT=15672

# =============================================================================
# SIFEN - URLs dos WebServices
# =============================================================================
# Ambiente de TESTE (homologação):
URL_SIFEN_CONSULTA_LOTE=https://ekuatia.set.gov.py/sifen/ws/async/consulta-lote-v150.wsdl
URL_SIFEN_RECEBE_LOTE=https://ekuatia.set.gov.py/sifen/ws/async/recibe-de-v150.wsdl
URL_SIFEN_QR=https://ekuatia.set.gov.py/consultas/qr?
URL_SIFEN_EVENTO=https://ekuatia.set.gov.py/sifen/ws/eventos/evento-v150.wsdl

# =============================================================================
# CONFIGURAÇÕES DO WORKER
# =============================================================================
# Tempo de delay (ms) antes de consultar status após envio (padrão: 30000 = 30s)
DELAY_TTL_MS=30000
